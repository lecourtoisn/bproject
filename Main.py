from CustomClient import CustomClient
from MessengerBot import Blackjack, MultiCommandBot

if __name__ == '__main__':
    # client = CustomClient()
    # messenger_bot = (client)
    # client.listen()
    # client.logout()

    m = MultiCommandBot()
    m.on_draw(None)
